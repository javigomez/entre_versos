/** @deprecated Usa content-repository: admite fuentes YAML y TypeScript. */
export {
  createContentRepository as createYamlContentRepository,
  createValidatedContentRepository as createValidatedYamlContentRepository,
} from './content-repository';
